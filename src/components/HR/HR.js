import React from 'react'

const styles = {
  height: "1px",
  backgroundColor: "var(--bgLight)",
  width: "100%"
}
const HR = ({classes}) => {
  return (
    <div style={{...styles}} className={classes}>
      
    </div>
  )
}

export default HR
